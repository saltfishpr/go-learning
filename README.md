# msgp example

This is a simple example of msgp.

[msgp](https://github.com/tinylib/msgp) is a code generation tool and serialization library
for [MessagePack](http://msgpack.org).

Run `go mod tidy`, `make generate`, `make build`, `make run` to see output.
